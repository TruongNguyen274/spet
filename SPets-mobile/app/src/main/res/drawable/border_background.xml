<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <!-- Màu nền của LinearLayout -->
    <solid android:color="#FFFFFF" /> <!-- Đổi màu nền tại đây -->

    <!-- Viền của LinearLayout -->
    <stroke
        android:width="1dp"
        android:color="#FFFFFF" />

    <!-- Góc bo viền -->
    <corners android:radius="4dp" /> <!-- Độ cong của góc viền -->
</shape>
